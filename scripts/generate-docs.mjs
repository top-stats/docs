// Pages are no longer automatically generated, instead pages are manually grouped due to limitations with grouping with FumaDocs and OpenAPI Schema.
// Open API Schema is still used see referenced API pages.

/*
void generateFiles({
  input: ['./openapi.json'], // Path to your OpenAPI schema
  output: './content/docs/reference', // Output folder for docs
  includeDescription: true, // Include descriptions for each endpoint
  groupBy: 'tag',
});
 */
